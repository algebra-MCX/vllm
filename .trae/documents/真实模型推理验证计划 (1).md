# 修正与验证计划

## 1. 代码清理 (Hotfix)
### `vllm/forward_context.py`
- **目标**: 移除我错误引入的冗余 MoE 字段，恢复代码整洁，只保留 KV Cache 相关的改动。
- **操作**:
    - 删除 `all_moe_layers: list[str] | None = None`。
    - 删除 `moe_layer_index: int = 0`。
    - 在 `create_forward_context` 中，删除对应的初始化逻辑（即删除 `all_moe_layers=vllm_config.compilation_config.static_all_moe_layers`）。
    - **保留** `remaining_moe_layers`，因为它是现有 MoE 代码所依赖的。

## 2. 真实模型验证
### `tests/test_real_model_inference.py`
- **目标**: 使用本地真实的 `Qwen1.5-MoE-A2.7B` 模型进行端到端推理测试。
- **内容**:
    - 加载模型 `/home/ffc3/mcx/models/Qwen1.5-MoE-A2.7B`。
    - 运行简单的推理（Prompt: "Hello"）。
    - 验证输出是否正常，无报错。

## 3. 执行顺序
1.  **清理代码**: 修复 `forward_context.py`。
2.  **创建测试**: 编写真实模型推理脚本。
3.  **运行验证**: 执行推理脚本。
4.  **最终交付**: 确认代码干净且功能可用。