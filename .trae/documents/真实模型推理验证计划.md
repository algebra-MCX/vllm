# 真实模型验证计划

## 1. 验证目标
使用本地真实的 `Qwen1.5-MoE-A2.7B` 模型进行端到端推理测试，以验证：
1.  **稳定性**: 确认修改后的 `Attention` 和 `ForwardContext` 逻辑在真实模型加载和推理过程中不会崩溃。
2.  **正确性**: 确认模型生成的文本逻辑通顺，没有乱码或重复，证明 KV Cache 更新逻辑正确。
3.  **性能 (可选)**: 观察冷启动阶段的耗时情况（虽然受加载权重影响较大，但可作为参考）。

## 2. 测试脚本 (`tests/test_real_model_inference.py`)
创建一个简单的 vLLM 推理脚本：
- **模型路径**: `/home/ffc3/mcx/models/Qwen1.5-MoE-A2.7B`
- **配置**:
    - `trust_remote_code=True`
    - `enforce_eager=False` (确保触发 CUDA Graph / torch.compile 路径，如果支持)
    - `gpu_memory_utilization=0.9`
- **输入**: 一组简单的 Prompts (e.g., "Hello, who are you?", "Explain quantum physics briefly.")
- **输出**: 打印生成的文本和 token 数量。

## 3. 执行步骤
1.  **创建脚本**: 编写 `tests/test_real_model_inference.py`。
2.  **执行推理**: 在 GPU 环境下运行该脚本。
3.  **结果分析**:
    - 检查是否成功运行无报错。
    - 检查输出文本是否正常。

## 4. 预期结果
- 脚本成功加载模型。
- 控制台输出生成的回答，且内容连贯。
- 没有 `AssertionError` 或 `RuntimeError` 抛出。